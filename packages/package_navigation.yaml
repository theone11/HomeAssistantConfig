#####################################
########## E N T I T I E S ##########
#####################################

template:
  - sensor:
    - name: work_dan_ui
      unit_of_measurement: 'm'
      state: "{{ states('sensor.work_dan') | round(0, default = '') }}"
    - name: work_aviva_ui
      unit_of_measurement: 'm'
      state: "{{ states('sensor.work_aviva') | round(0, default = '') }}"
    - name: kfar_yona_ui
      unit_of_measurement: 'm'
      state: "{{ states('sensor.kfar_yona') | round(0, default = '') }}"
    - name: petach_tikva_ui
      unit_of_measurement: 'm'
      state: "{{ states('sensor.petach_tikva') | round(0, default = '') }}"

###########################################
########## A U T O M A T I O N S ##########
###########################################

# automation:

###################################
########## S C R I P T S ##########
###################################

# script:
# # ---------------------
